type topRisk = {
    //
};

type rootCause = {
    //
};

type recomendation = {
    //
};

type baseType = {
    topRisk: topRisk;
    rootCause: rootCause;
    recomendation: recomendation;
};

export type RPROType = baseType;
