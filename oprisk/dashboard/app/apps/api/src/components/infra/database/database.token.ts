export class DatabaseToken {
    static readonly Schema: unique symbol = Symbol('oprisk');
    static readonly DashboardTablePrefix: unique symbol = Symbol('');

    // static readonly MISSchema: unique symbol = Symbol('mis');
    // static readonly DDMSchema: unique symbol = Symbol('ddm');
    // static readonly GLSchema: unique symbol = Symbol('gl');
    // static readonly CFASchema: unique symbol = Symbol('cfa');
}
