export class LproMonitorToken {
    static readonly LproMonitorServiceInterface: unique symbol = Symbol('LproMonitorServiceInterface');

    static readonly LproRepositoryInterface: unique symbol = Symbol('LproRepositoryInterface');

}