export class DashboardLproToken {
    static readonly Dashboard: unique symbol = Symbol('Dashboard');
    static readonly DashboardLproServiceInterface: unique symbol = Symbol(
        'DashboardLproServiceInterface',
    );
    static readonly DashboardRepositoryInterface: unique symbol = Symbol(
        'DashboardRepositoryInterface',
    );
}
